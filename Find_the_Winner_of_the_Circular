def findTheWinner(self, n, k):
        """
        :type n: int
        :type k: int
        :rtype: int
        """
        def helper(n,k):
            if n==1:
                return 0
            prevcall=helper(n-1,k)
            return (prevcall+k)%n
        return helper(n,k)+1
